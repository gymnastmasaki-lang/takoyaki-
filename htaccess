# 粉もん屋 八 - .htaccess
# 検索エンジンのクローラーをブロック（セルフオーダー専用）

# X-Robots-Tagヘッダーを全ページに追加
<FilesMatch "\.(html|htm)$">
    Header set X-Robots-Tag "noindex, nofollow"
</FilesMatch>

# 特定のクローラーをブロック（オプション）
# RewriteEngine On
# RewriteCond %{HTTP_USER_AGENT} (googlebot|bingbot|yandex|baiduspider) [NC]
# RewriteRule .* - [F,L]

# セキュリティ強化: ディレクトリリスティングを無効化
Options -Indexes

# セキュリティ強化: サーバー情報を隠す
ServerSignature Off
